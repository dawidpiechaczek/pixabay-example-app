apply plugin: 'com.android.library'
apply from: "$rootDir/common-ui.gradle"

dependencies {
    implementation project(':core:ui')
    api project(':photos:data')

    testImplementation(project(':shared-test'))
}